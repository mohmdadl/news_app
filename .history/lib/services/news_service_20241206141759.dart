import 'package:flutter/material.dart';

class NewsService extends StatelessWidget {
  const NewsService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}